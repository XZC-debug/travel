# GitHub Pages 配置文件
# 这个文件告诉 GitHub Pages 如何处理你的站点

# 站点基本信息
title: 新墨西哥行程规划
description: 2025年11月25-29日 新墨西哥自驾游完整行程和地图
url: https://xzc-debug.github.io/travel

# GitHub Pages 设置
source: docs
encoding: utf-8

# 注意：不设置 theme，因为使用纯 HTML 站点
# 如果要使用主题，可选择：minima, jekyll-theme-slate, jekyll-theme-cayman 等

# 不处理以下文件
exclude:
  - Gemfile
  - Gemfile.lock
  - README.md
  - .gitignore
  - node_modules
  - vendor

# 这是一个纯 HTML 站点，所有样式都在 HTML 文件中定义
# GitHub Pages 会直接发布这些 HTML 文件，无需进行 Jekyll 处理
